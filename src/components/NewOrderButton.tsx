export function NewOrderButton() {
  return (
    <div className="px-6 mt-6">
      <button className="w-full py-3 rounded-lg bg-primary text-white font-semibold hover:bg-orange-500 transition">
        + NUOVO ORDINE
      </button>
    </div>
  );
}
